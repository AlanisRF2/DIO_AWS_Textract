[project]
name = "ocr-lista-escolar"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "boto3>=1.43.72",
    "boto3-stubs[textract]>=1.43.72",
]

[project.scripts]
ocr-lista-escolar = "ocr_lista_escolar:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
